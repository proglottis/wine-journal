<h1><%= @wine.name %></h1>

<% @wine.ratings.each do |rating| %>
<div class="box">
Nobody rated this wine <%= rating.value %>, <%= time_ago_in_words rating.created_at%> ago.
</div>
<% end %>

<%= link_to 'Edit', edit_wine_path(@wine) %> |
<%= link_to 'Back', wines_path %>
